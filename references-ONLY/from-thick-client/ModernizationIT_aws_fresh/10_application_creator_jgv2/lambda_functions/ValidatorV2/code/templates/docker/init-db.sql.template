-- Database initialization for {{APPLICATION_NAME}}
-- This file is automatically executed when the database container starts

-- Create extensions if needed
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Sample initialization (tables created by Hibernate DDL auto)
-- Add any custom initialization here

-- Example: Insert default data
-- INSERT INTO accounts (acct_no, last_name, first_name)
-- VALUES ('00000001', 'Sample', 'Account');

-- Log initialization
DO $$
BEGIN
  RAISE NOTICE 'Database initialized successfully for {{APPLICATION_NAME}}';
END $$;
